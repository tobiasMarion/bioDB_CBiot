DATABASE_URL="postgresql://postgres:postgres@localhost:5432/bioDB?schema=public"

# Shared secret used to sign (mock login) and validate JWTs. In deploy the
# server injects the same value across all projects. Locally any value works,
# as long as it is identical wherever tokens are signed and verified.
JWT_SECRET=dev-secret
